<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shahityaa.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Styles n&apos; Smiles"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:textColorSecondary="@android:color/background_light"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page2" />
        <activity android:name=".page3" />
        <activity android:name=".about" />
        <activity android:name=".services" />
        <activity android:name=".appointment" />
    </application>

</manifest>